# @career-companion/application

Canonical application-layer contracts for Career Companion.

This package defines the commands, queries, use-case contracts, execution context, policy contracts, validation contracts, and result envelopes exposed by the platform. It does not contain business algorithms, repository implementations, retrieval implementations, persistence implementations, HTTP, GraphQL, REST, AI, infrastructure, or orchestration logic.

