# Interview Intelligence

Deterministic Interview Intelligence Engine for Career Companion.

This package transforms an interview question, target context, and validated Career Knowledge into an immutable `InterviewModel` backed by a canonical `CareerArtifact` of type `InterviewGuide`. It does not generate polished AI-written answers, render documents, persist data, call APIs, or integrate with infrastructure.
