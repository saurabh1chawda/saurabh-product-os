# Job Intelligence

`@career-companion/job-intelligence` deterministically transforms raw job descriptions into immutable, explainable hiring-opportunity artifacts.

It produces `JobModel`, `HiringModel`, `EvaluationFramework`, and `JobMatchReport` models. It does not perform AI execution, prompting, rendering, persistence, retrieval, infrastructure, or application runtime behavior.
