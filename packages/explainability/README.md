# Explainability

Deterministic explanation models and builders for Career Companion decisions.

This package explains decisions. It does not make decisions, invoke AI, persist state, expose APIs, or depend on infrastructure.
