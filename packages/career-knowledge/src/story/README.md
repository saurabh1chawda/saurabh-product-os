# Story

Reusable professional experience aggregate.

This module contains pure domain behavior only. It uses typed references instead of object graphs and does not contain persistence, repositories, APIs, workflow logic, AI, or infrastructure.
