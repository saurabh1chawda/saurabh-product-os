# EvidenceReference

EvidenceReference is the aggregate root for a reference to authoritative support for career facts, claims, achievements, and credentials.

This aggregate is type-only in Increment B1. No persistence, evidence storage, validation service, or traversal behavior is implemented.

