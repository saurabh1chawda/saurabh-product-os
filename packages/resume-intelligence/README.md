# Resume Intelligence

Deterministic Resume Intelligence Engine for Career Companion.

This package transforms validated career knowledge into immutable `ResumeModel` objects. It does not render DOCX, PDF, Markdown, HTML, invoke AI, persist data, or integrate with infrastructure.
