# Portfolio Intelligence

Deterministic Portfolio Intelligence Engine for Career Companion.

This package transforms validated Career Knowledge into immutable `PortfolioModel` objects backed by a canonical `CareerArtifact` of type `Portfolio`. It does not render HTML, React, Markdown, PDF, DOCX, invoke AI, persist data, or integrate with infrastructure.
