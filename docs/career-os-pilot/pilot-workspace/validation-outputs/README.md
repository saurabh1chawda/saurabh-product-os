# Pilot Validation Outputs

This folder is for pilot-level validation snapshots and summaries.

Validation outputs that include private application data must remain local-first and uncommitted. Public validation summaries should contain only pass/fail status, counts, and anonymized findings.
