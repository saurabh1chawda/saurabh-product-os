# A16.2k - Portfolio Workspace Release-Candidate Staging Review

## Document Control

- Status: Ready for commit approval
- Date: 2026-08-10
- Baseline: `fe4b0be7ced9978f8f0ee9dc7e8683dd576d2204`
- Baseline message: `fix(workspace): restore shared artifact baseline contracts`
- Toolchain: Node `v22.23.2`, pnpm `11.9.0`

Supersession note: A16.2k.1 records that this staged approval no longer describes the current dirty working tree because the A16.2k release candidate and CI follow-up were committed, the current index is empty, and later unstaged authorization/idempotency work now requires explicit scope approval before any further release-candidate commit.

## Executive Summary

The exact Portfolio Workspace A10-A16 release candidate has been staged on top of the approved shared baseline repair commit. The staged index includes Portfolio Workspace Domain, Application, Infrastructure-memory, PostgreSQL persistence, runtime composition, API presentation/host code, migration artifacts, shared release-support manifests and lockfile, CI workflow, architecture boundary tests, and A10-A16 architecture documentation.

Unrelated local work remains unstaged.

Database-free validation passed after staging. The immediately preceding live PostgreSQL 17 validation after A16.2j is accepted because the staged production source is the validated A16.2j source plus release documentation/staging records.

## Baseline and Index

Verified baseline:

```text
fe4b0be7ced9978f8f0ee9dc7e8683dd576d2204
```

The index was empty before staging. After exact staging, the staged index contains 194 files before this A16.2k document and 195 files after this document is added.

## Staged Inventory

Domain:

- `packages/portfolio-workspace/**`

Application:

- `packages/portfolio-workspace-application/**`

Infrastructure-memory:

- `packages/infrastructure-memory/README.md`
- `packages/infrastructure-memory/package.json`
- `packages/infrastructure-memory/src/index.ts`
- `packages/infrastructure-memory/src/portfolio-workspace/**`
- `packages/infrastructure-memory/tests/portfolio-workspace-repository.test.ts`
- `packages/infrastructure-memory/tsconfig.json`

PostgreSQL persistence and runtime:

- `packages/infrastructure/README.md`
- `packages/infrastructure/package.json`
- `packages/infrastructure/src/index.ts`
- `packages/infrastructure/tsconfig.json`
- `packages/infrastructure/drizzle.config.ts`
- `packages/infrastructure/src/portfolio-workspace/**`
- `packages/infrastructure/tests/portfolio-workspace-*`

API, presentation, and host:

- `apps/api/README.md`
- `apps/api/package.json`
- `apps/api/src/index.ts`
- `apps/api/src/portfolio-workspace/**`
- `apps/api/tests/portfolio-workspace-*`
- `apps/api/tsconfig.json`

Shared release support:

- `pnpm-workspace.yaml`
- `pnpm-lock.yaml`

Architecture tests:

- `tests/architecture/dependency-boundaries.test.ts`

CI:

- `.github/workflows/portfolio-workspace-release-validation.yml`

Documentation:

- `docs/architecture/A10*`
- `docs/architecture/A11*`
- `docs/architecture/A12*`
- `docs/architecture/A13*`
- `docs/architecture/A14*`
- `docs/architecture/A15*`
- `docs/architecture/A16*`
- `docs/architecture/adr/ARCH-004-execution-workspace-architecture.md`

## Migration Artifact Integrity

The complete Drizzle migration set is staged:

- `packages/infrastructure/drizzle/portfolio-workspace/0000_shocking_firebrand.sql`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/0000_snapshot.json`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/_journal.json`

The migration creates the single `portfolio_executions` JSONB aggregate snapshot table. Runtime metadata-table isolation remains implemented in runtime source, not in the schema migration.

## Lockfile Decision

`pnpm-lock.yaml` is staged because frozen install passes with the staged release manifests. The lockfile reflects:

- Portfolio Workspace packages;
- Infrastructure Drizzle/PostgreSQL dependencies;
- API live-host test dependencies;
- Infrastructure-memory Portfolio Workspace dependencies;
- `expect-type` `1.3.0` override.

No excluded local manifest dependency was identified in the staged lockfile assessment.

## Explicitly Excluded Files

The following local files remain unstaged and excluded:

- `packages/career-intelligence/tests/deterministic-services.test.ts`
- `packages/career-knowledge/tests/aggregate-behavior.test.ts`
- `packages/decision-engine/tests/pipelines.test.ts`
- `packages/decision-model/tests/decision-model.test.ts`
- `packages/kernel/tests/kernel-primitives.test.ts`
- `e-2-complete`
- `"tatus --short --untracked-files=all"`

## Validation

Frozen lockfile:

- `pnpm install --frozen-lockfile`: passed

Package validation:

- `@career-companion/portfolio-workspace`: build, lint, typecheck, test passed
- `@career-companion/portfolio-workspace-application`: build, lint, typecheck, test passed
- `@career-companion/infrastructure-memory`: build, lint, typecheck, test passed
- `@career-companion/infrastructure`: build, lint, typecheck, test, `db:check` passed
- `@career-companion/api`: build, lint, typecheck, test passed

Root validation:

- `pnpm build`: passed
- `pnpm lint`: passed
- `pnpm typecheck`: passed
- `pnpm test`: passed

Live PostgreSQL 17 validation accepted from the immediately preceding authoritative A16.2j run:

- Repository suite: 17/17 passed
- Runtime suite: 9/9 passed
- API host suite: 8/8 passed

## Clean-Checkout Reproducibility Assessment

The staged index is internally consistent:

- frozen lockfile install passed;
- staged manifests and lockfile align;
- migration artifacts are complete;
- CI workflow references existing scripts;
- no staged path imports excluded local files;
- unrelated local files remain unstaged.

The current working tree still contains excluded local test files, so the root test command observed a superset of local tests. The staged release candidate itself does not depend on those excluded files.

## Commit Structure Recommendation

Use one atomic Portfolio Workspace release-candidate commit after approval.

Rationale:

- the release spans Domain, Application, Infrastructure, API, migrations, runtime, docs, and CI;
- shared manifests and lockfile must move with the source graph;
- migration artifacts must move with the Infrastructure implementation;
- a single commit minimizes intermediate non-buildable states.

Recommended message:

```text
feat(portfolio-workspace): add release-candidate runtime and persistence stack
```

## Remaining Risks

Remaining release risks after commit approval:

- hosted GitHub Actions A16.2 run is not yet proven;
- branch protection is not yet configured;
- production auth, durable idempotency, observability, and deployment wiring remain later release-readiness work.

## Readiness Decision

READY FOR COMMIT APPROVAL
