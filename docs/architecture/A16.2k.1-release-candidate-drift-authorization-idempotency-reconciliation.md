# A16.2k.1 - Release Candidate Drift and Authorization/Idempotency Reconciliation

## Document Control

- Status: READY FOR SCOPE APPROVAL
- Date: 2026-08-11
- Repository: `C:\Users\saura\saurabh-product-os`
- Baseline under review: A16.2k release-candidate staging approval
- A16.2k baseline commit: `fe4b0be7ced9978f8f0ee9dc7e8683dd576d2204`
- Current HEAD inspected: `cd9ff406923e458e655ea607df19fe45d586dbdc`

## Executive Summary

A16.2k's original staged approval no longer describes the current working-tree state.

The A16.2k release candidate was committed as `2f2de54d feat(portfolio-workspace): complete release candidate architecture`, and a later CI workflow fix was committed as `cd9ff406 fix(ci): activate pnpm before release validation`.

The current index is empty, not a 195-file staged release-candidate index. On top of current HEAD, the working tree contains material unstaged Portfolio Workspace changes for production authorization ownership and durable idempotency persistence. These changes have live validation evidence, but that evidence proves behavior, not release-scope approval.

Commit approval is therefore not preserved for the current tree. The next action is explicit release-scope approval and exact restaging.

## Current Git State

Current HEAD:

```text
cd9ff406923e458e655ea607df19fe45d586dbdc
```

Recent commits:

```text
cd9ff406 fix(ci): activate pnpm before release validation
2f2de54d feat(portfolio-workspace): complete release candidate architecture
fe4b0be7 fix(workspace): restore shared artifact baseline contracts
0d2fa4c1 docs(architecture): finalize phase 2 milestone
24495a15 feat(career-companion): add application planner
```

Index state:

- `git diff --cached --name-status`: empty.
- `git diff --cached --stat`: empty.
- `git diff --cached --check`: passed.
- Current staged file count: `0`.
- Current cached insertion/deletion totals: `0/0`.

There are two commits after the A16.2k baseline `fe4b0be7`:

- `2f2de54d`: committed the Portfolio Workspace release-candidate architecture.
- `cd9ff406`: committed the GitHub Actions pnpm activation/cache-order correction.

## A16.2k Comparison

A16.2k recorded:

- 195 staged files.
- One Portfolio Workspace release-candidate staged index.
- One PortfolioExecution migration only:
  - `0000_shocking_firebrand.sql`
  - `meta/0000_snapshot.json`
  - `meta/_journal.json`
- API host live evidence accepted as 8/8.
- Production authorization as a remaining later release-readiness condition.
- Durable public mutation idempotency as a remaining later release-readiness condition.

Current state:

- No files are staged.
- A16.2k content is already committed in `2f2de54d`.
- Current working tree has additional unstaged Portfolio Workspace changes.
- API host live evidence is now 9/9.
- Durable idempotency live evidence now exists as a separate 6/6 suite.
- The migration journal now has two entries in the working tree.

Selected comparison classification: release-candidate approval is superseded for the current tree because the current index is empty and the working tree includes material unstaged production behavior beyond A16.2k.

## Authorization Inventory

Authorization-related unstaged production/domain/application files:

- `apps/api/src/portfolio-workspace/host/PortfolioWorkspaceApiHost.ts`
- `apps/api/src/portfolio-workspace/internal/authorization.ts`
- `apps/api/src/portfolio-workspace/internal/get-handler.ts`
- `apps/api/src/portfolio-workspace/internal/index.ts`
- `apps/api/src/portfolio-workspace/internal/initialize-handler.ts`
- `apps/api/src/portfolio-workspace/internal/production-authorization.ts`
- `apps/api/src/portfolio-workspace/presentation/mappers.ts`
- `packages/portfolio-workspace/src/aggregate/PortfolioExecution.ts`
- `packages/portfolio-workspace/src/facts/PortfolioExecutionFacts.ts`
- `packages/portfolio-workspace/src/index.ts`
- `packages/portfolio-workspace/src/value-objects/PortfolioWorkspaceAuthorizationResourceReference.ts`
- `packages/portfolio-workspace-application/src/inputs/InitializePortfolioExecutionInput.ts`
- `packages/portfolio-workspace-application/src/inputs/ResolvePortfolioExecutionAuthorizationResourceInput.ts`
- `packages/portfolio-workspace-application/src/results/ResolvePortfolioExecutionAuthorizationResourceResult.ts`
- `packages/portfolio-workspace-application/src/services/InitializePortfolioExecutionApplicationService.ts`
- `packages/portfolio-workspace-application/src/services/ResolvePortfolioExecutionAuthorizationResourceApplicationService.ts`
- `packages/infrastructure-memory/src/portfolio-workspace/InMemoryPortfolioExecutionRepository.ts`
- `packages/infrastructure/src/portfolio-workspace/persistence/PortfolioExecutionRecord.ts`
- `packages/infrastructure/src/portfolio-workspace/persistence/PortfolioExecutionRecordMapper.ts`
- `packages/infrastructure/src/portfolio-workspace/runtime/PortfolioWorkspaceRuntime.ts`

Authorization-related tests and docs are also present in the working tree:

- API host/internal handler tests.
- Portfolio Workspace Domain/Application/Infrastructure tests.
- A16.4/A16.4a/A16.4b/A16.4c architecture documents.

## Authorization Production Behavior

Current unstaged authorization work adds real production behavior:

- `PortfolioWorkspaceAuthorizationResourceReference` is a Domain value object.
- `PortfolioExecution` stores an immutable authorization resource reference.
- Initialization input and initialization fact include that reference.
- The durable PortfolioExecution record version is bumped from `1` to `2`.
- The record mapper writes and reads `aggregatePayload.authorizationResourceReference`.
- The API host can default to `PortfolioWorkspaceProductionAuthorization`.
- Initialization derives ownership from the trusted principal through the authorization boundary.
- Get-by-ID resolves persisted ownership and denies mismatched principals.

Ownership is persisted inside the PortfolioExecution aggregate JSONB payload. No separate authorization table is present.

## Authorization Classification

The production authorization work is unstaged local work relative to current HEAD. It was not included in A16.2k's 195-file staging narrative or its one-migration inventory.

Classification: UNSTAGED POST-A16.2K PRODUCTION SCOPE DRIFT.

## Idempotency Inventory

Idempotency-related unstaged API files:

- `apps/api/src/portfolio-workspace/idempotency/contracts.ts`
- `apps/api/src/portfolio-workspace/idempotency/index.ts`
- `apps/api/src/index.ts`
- `apps/api/tests/portfolio-workspace-idempotency.test.ts`

Idempotency-related unstaged Infrastructure files:

- `packages/infrastructure/package.json`
- `packages/infrastructure/src/index.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/PortfolioWorkspaceIdempotencyPersistenceContracts.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/PortfolioWorkspaceIdempotencyRecordMapper.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/PostgresPortfolioWorkspaceIdempotencyStore.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/index.ts`
- `packages/infrastructure/src/portfolio-workspace/postgres/index.ts`
- `packages/infrastructure/src/portfolio-workspace/postgres/schema.ts`
- `packages/infrastructure/tests/portfolio-workspace-idempotency-integration-runner.ts`
- `packages/infrastructure/tests/portfolio-workspace-idempotency-postgres-live.test.ts`
- `packages/infrastructure/tests/portfolio-workspace-idempotency.test.ts`

Idempotency migration artifacts:

- `packages/infrastructure/drizzle/portfolio-workspace/0001_workable_molly_hayes.sql`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/0001_snapshot.json`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/_journal.json`

Idempotency documentation:

- `docs/architecture/A16.5-durable-mutation-idempotency-architecture.md`
- `docs/architecture/A16.5a-idempotency-contracts-replay-payload.md`
- `docs/architecture/A16.5b-durable-postgresql-idempotency-persistence.md`

## Idempotency Production Behavior

Current unstaged idempotency work adds production-capable Infrastructure storage:

- API-local idempotency contract objects and replay payload vocabulary.
- Infrastructure persistence contracts.
- `PostgresPortfolioWorkspaceIdempotencyStore`.
- `portfolio_workspace_idempotency_records` Drizzle table.
- Migration #2 for idempotency table, constraints, and indexes.
- Live idempotency integration runner and tests.

It is not yet wired into mutation handlers. The current handler search shows no public idempotency reservation/check/replay path in initialize/get handlers. Therefore durable storage exists, but mutation idempotency enforcement remains incomplete until a later coordinator/handler integration slice.

## Idempotency Classification

The idempotency implementation and migration are unstaged local work relative to current HEAD. They were not included in the A16.2k one-migration inventory.

Classification: UNSTAGED POST-A16.2K PRODUCTION SCOPE DRIFT.

## Migration Drift

A16.2k approved one Portfolio Workspace migration:

- `0000_shocking_firebrand.sql`

Current working tree has two migrations:

- `0000_shocking_firebrand.sql`
- `0001_workable_molly_hayes.sql`

The new migration creates `portfolio_workspace_idempotency_records`. No authorization/ownership table was added. Authorization ownership is stored in the aggregate payload and changes record compatibility through `PORTFOLIO_EXECUTION_RECORD_VERSION = 2`.

The current working-tree `_journal.json` has an added `idx: 1` entry for `0001_workable_molly_hayes`.

## Package Script Drift

`packages/infrastructure/package.json` adds:

```json
"test:integration:portfolio-workspace-idempotency": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types tests/portfolio-workspace-idempotency-integration-runner.ts"
```

The earlier scripts remain present:

- `test:integration:portfolio-workspace`
- `test:integration:portfolio-workspace-runtime`

`apps/api/package.json` has no current working-tree diff.

## Manifest and Lockfile Drift

No current working-tree diff was observed for `pnpm-lock.yaml` or `pnpm-workspace.yaml` in this investigation snapshot.

The observed package-script drift is in `packages/infrastructure/package.json`.

## API Host Test Drift

The API host live suite now includes a ninth scenario:

```text
enforces production authorization against persisted ownership across principal boundaries
```

The same file also includes current corrections around migration-count assertions and persisted authorization resource privacy assertions.

This materially differs from A16.2k's accepted API host evidence of 8/8.

## Runtime Test Drift

Runtime live evidence is now 9/9. Runtime/source drift includes exposure of `resolvePortfolioExecutionAuthorizationResource` and tests expecting idempotency contracts not to leak through the runtime boundary.

## New Live Evidence

Authoritative manual Node 22 evidence supplied by the user:

- Node: `v22.23.2`
- pnpm: `11.9.0`
- Repository live integration: 17/17 passed.
- Runtime live integration: 9/9 passed.
- API host live integration: 9/9 passed.
- Idempotency PostgreSQL live integration: 6/6 passed.

This evidence validates behavior in the current tree, but does not by itself approve scope expansion.

## Selected Release-Scope Model

Selected model: MODEL C plus current-index replacement caveat.

The original A16.2k release candidate has already been committed and the current index is empty. The new authorization and idempotency work is unstaged future/release-readiness work on top of current HEAD.

Because the current index no longer contains the A16.2k-approved staged candidate, A16.2k's staged commit approval does not apply to any current staged state.

## Architecture Scope Assessment

Production authorization was explicitly deferred by A15/A16 and is now implemented as unstaged A16.4-series work. It should receive explicit scope approval before inclusion in any release-candidate commit.

Durable idempotency was explicitly required before public mutation exposure and is now partly implemented as A16.5-series work. It provides contracts, storage, migration, and live validation, but it is not wired into mutation handlers and does not close public mutation idempotency alone. It should be approved as a distinct later-scope commit or held until the transaction/coordinator slice is ready.

## A16 Remaining-Condition Impact

If approved, A16.4 authorization work materially reduces the production authorization blocker for internal initialize/get flows, but provider authentication and public exposure policy remain outstanding.

A16.5b idempotency storage reduces the durable idempotency blocker, but public mutation remains blocked until idempotency is enforced through an atomic mutation/idempotency transaction boundary.

## Commit Approval Status

Commit approval is not preserved for the current tree.

Reasons:

- Current index is empty.
- A16.2k-approved staged index no longer exists.
- Current working tree contains unstaged production authorization changes not described by A16.2k.
- Current working tree contains unstaged durable idempotency migration/storage changes not described by A16.2k.
- Current live evidence proves current behavior but not release-scope approval.

## Required Next Action

Run a dedicated scope-approval/restaging slice that decides whether to:

1. Commit only current HEAD state as already done and keep A16.4/A16.5 work for later.
2. Create separate A16.4 authorization commit(s).
3. Create separate A16.5 idempotency commit(s).
4. Expand the current release-candidate scope and restage the combined tree deliberately.

No commit should be made from the current dirty tree until that decision is explicit.

A16.2k.2 selected the scope model: authorization should proceed as a separate production release commit candidate, while idempotency remains a separate incomplete production-readiness workstream. No authorization staging occurred in A16.2k.2 because an authorization-only clean candidate excluding idempotency was not yet proven.

## Guardrail Verification

This investigation:

- did not commit;
- did not push;
- did not amend;
- did not reset;
- did not clean;
- did not stash;
- did not stage or unstage files;
- did not regenerate migrations;
- did not modify authorization/idempotency/runtime/schema/handler production code.

## Readiness Decision

READY FOR SCOPE APPROVAL
