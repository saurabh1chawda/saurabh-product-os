# A16.2g - Resume Intelligence Manifest vs Lockfile Baseline Reconciliation

## 1. Executive Summary

A16.2g resolves the A16.2f manifest/lockfile question.

The local `packages/resume-intelligence/package.json` hunk adding `@career-companion/career-artifacts` is not the correct baseline repair. Committed `resume-intelligence` source does not import `@career-companion/career-artifacts`, and the committed package boundary test rejects that dependency.

The selected classification is: HEAD LOCKFILE BASELINE DEFECT.

The correct next repair is a narrowly reviewed `pnpm-lock.yaml` correction that removes the stale `@career-companion/career-artifacts` dependency from the `packages/resume-intelligence` importer, while preserving the already staged `career-artifacts` and `explainability` package repair.

No `resume-intelligence` hunk was staged.

Forward reference: A16.2h broadened the evidence set beyond committed `resume-intelligence` alone and found committed ATS, Opportunity Intelligence, and Career Decision consumers already require `ResumeModel.artifact`. That later review supersedes this lockfile-only conclusion: the `resume-intelligence` artifact repair is the correct baseline reconciliation, and the existing lockfile importer entry becomes consistent once that repair is included.

## 2. HEAD Manifest State

`HEAD:packages/resume-intelligence/package.json` dependencies are:

- `@career-companion/application`
- `@career-companion/career-intelligence`
- `@career-companion/career-knowledge`
- `@career-companion/decision-engine`
- `@career-companion/decision-model`
- `@career-companion/explainability`
- `@career-companion/kernel`

It does not declare `@career-companion/career-artifacts`.

## 3. HEAD Lockfile State

`HEAD:pnpm-lock.yaml` contains a `packages/resume-intelligence` importer with the same dependencies as the manifest plus a stale extra dependency:

- `@career-companion/career-artifacts`

This mismatch predates A10-A16 and exists in committed baseline lockfile state.

## 4. Committed Resume-Intelligence Source Imports

`git grep "@career-companion/career-artifacts" HEAD -- packages/resume-intelligence` returned no matches.

Committed `resume-intelligence` source and tests do not import `@career-companion/career-artifacts`.

No symbols from the staged `career-artifacts` package are required by committed `resume-intelligence` source.

## 5. Git History Findings

`git log -S"@career-companion/career-artifacts" --all -- packages/resume-intelligence/package.json` returned no commits.

`git log -p -- packages/resume-intelligence/package.json` shows the package was introduced without `@career-companion/career-artifacts`.

`git log -S"@career-companion/career-artifacts" --all -- pnpm-lock.yaml` shows the lockfile dependency history across earlier Career Companion commits. The stale `resume-intelligence` importer entry was present in the committed lockfile without the matching committed manifest dependency.

History supports a lockfile baseline defect, not a lost manifest hunk.

## 6. Local Manifest Hunk Assessment

The local `packages/resume-intelligence/package.json` diff contains one hunk:

- adds `@career-companion/career-artifacts`: classified as unrelated to committed `resume-intelligence` source and not approved by committed package boundary tests.

No other `resume-intelligence/package.json` hunk exists.

## 7. Model A Simulation

Model A simulation:

`HEAD + staged career-artifacts + staged explainability + only the resume-intelligence package.json career-artifacts dependency hunk`

Results:

- `pnpm install --frozen-lockfile`: passed.
- `pnpm --filter @career-companion/resume-intelligence build`: passed.
- `pnpm --filter @career-companion/resume-intelligence lint`: passed.
- `pnpm --filter @career-companion/resume-intelligence typecheck`: passed.
- `pnpm --filter @career-companion/resume-intelligence test`: failed.

Failure:

The committed package boundary test expected seven dependencies and rejected the added `@career-companion/career-artifacts` dependency.

Conclusion:

Model A reproduces the lockfile but violates committed package boundary expectations.

## 8. Model B Simulation

Model B simulation:

`HEAD + staged career-artifacts + staged explainability + lockfile regenerated from committed manifests`

Temporary lockfile diff:

- removed only the stale `@career-companion/career-artifacts` entry from the `packages/resume-intelligence` importer.

Results:

- `pnpm install --lockfile-only --config.confirmModulesPurge=false`: passed in the temporary worktree.
- `pnpm install --frozen-lockfile`: passed in the temporary worktree after the lockfile correction.
- `pnpm --filter @career-companion/resume-intelligence build`: passed.
- `pnpm --filter @career-companion/resume-intelligence lint`: passed.
- `pnpm --filter @career-companion/resume-intelligence typecheck`: passed.
- `pnpm --filter @career-companion/resume-intelligence test`: passed, 1 file and 8 tests.

Root build then failed on a separate clean-HEAD type error:

- `packages/ats-intelligence/src/analysis/ATSParsingAnalyzer.ts` expects `ResumeModel.artifact`, but committed `ResumeModel` does not expose that property.

That failure is outside the manifest-vs-lockfile question and should be handled as a separate baseline reproducibility issue if broad root CI must validate clean HEAD.

## 9. Failure Classification

Classification: HEAD LOCKFILE BASELINE DEFECT.

Reason:

- committed manifest does not declare `career-artifacts`;
- committed source does not import `career-artifacts`;
- committed package boundary test rejects `career-artifacts`;
- temporary lockfile correction removes only the stale importer dependency and makes frozen install pass.

## 10. Selected Repair Model

Selected repair model: Model B - correct the lockfile.

Do not stage the local `resume-intelligence` manifest hunk.

Do not stage primary `pnpm-lock.yaml` until a clean regenerated lockfile candidate is reviewed and approved.

## 11. Partial Staging Decision

Partial staging was not performed.

The authorization conditions were not met because Model A failed committed package tests and the hunk is not supported by committed source imports.

## 12. Files Staged

No additional files were staged in A16.2g.

The index remains as staged in A16.2f:

- `packages/career-artifacts/**`
- `packages/explainability/**`

## 13. Files Explicitly Excluded

Explicitly excluded:

- `packages/resume-intelligence/package.json`
- `packages/resume-intelligence/src/**`
- `packages/resume-intelligence/tests/**`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`
- Portfolio Workspace A10-A16 files
- CI workflow files
- unrelated local work

## 14. Frozen Lockfile Result

Model A:

- frozen install passed, but the model failed committed package boundary tests.

Model B:

- temporary regenerated lockfile removed the stale importer entry;
- frozen install passed in the temporary worktree.

Primary worktree:

- no lockfile changes were staged.

## 15. career-artifacts Validation

A16.2f package validation remains authoritative:

- build: passed
- lint: passed
- typecheck: passed
- test: passed, 3/3

## 16. explainability Validation

A16.2f package validation remains authoritative:

- build: passed
- lint: passed
- typecheck: passed
- test: passed, 7/7

## 17. resume-intelligence Validation

Model A:

- build/lint/typecheck passed;
- test failed because the package boundary test rejects `@career-companion/career-artifacts`.

Model B:

- build passed;
- lint passed;
- typecheck passed;
- test passed, 8/8.

## 18. Root Validation

Model A root build failed on a separate clean-HEAD type error:

- `Property 'artifact' does not exist on type 'ResumeModel'` in `packages/ats-intelligence/src/analysis/ATSParsingAnalyzer.ts`.

Model B root build failed on the same separate clean-HEAD type error.

Root lint/typecheck/test were not run after the root build blocker.

## 19. Lockfile Decision

The primary `pnpm-lock.yaml` remains unstaged.

The next approved repair should generate or apply a narrow lockfile correction that removes `@career-companion/career-artifacts` only from the `packages/resume-intelligence` importer.

## 20. Baseline Repair Commit Scope

Current approved staged baseline package repair:

- `packages/career-artifacts/**`
- `packages/explainability/**`

Proposed additional repair, pending approval:

- narrow `pnpm-lock.yaml` correction for the stale `packages/resume-intelligence` importer.

Do not include:

- `packages/resume-intelligence/package.json`
- `packages/resume-intelligence/src/**`
- `packages/resume-intelligence/tests/**`

## 21. Files Created

- `docs/architecture/A16.2g-resume-intelligence-manifest-lockfile-baseline-reconciliation.md`

## 22. Files Modified

- `docs/architecture/A16.2f-shared-workspace-baseline-repair-validation.md`

## 23. Documentation Changes

A16.2g records the manifest/lockfile evidence, committed source import evidence, history, Model A/Model B simulations, selected repair model, staging decision, lockfile decision, and remaining root-validation blocker.

A16.2f is updated with a narrow forward reference to the A16.2g result.

## 24. Remaining Risks

The two-package baseline repair plus lockfile correction would fix the frozen-lockfile mismatch, but broad clean-HEAD root build still exposes a separate committed ATS/resume model mismatch.

That separate issue must be resolved before claiming broad clean-checkout root CI readiness.

## 25. Guardrail Verification

Preserved:

- no commit;
- no push;
- no Portfolio Workspace files staged;
- no unrelated `resume-intelligence` source/test files staged;
- no `resume-intelligence` manifest hunk staged;
- no primary lockfile staging;
- no reset/clean/stash;
- no local work discarded;
- no `career-artifacts` or `explainability` behavior changes;
- no CI changes;
- no A16.3 work.

Temporary validation worktrees and patch files were used outside the primary worktree.

## 26. Git Diff Summary

Staged index remains limited to:

- `packages/career-artifacts/**`
- `packages/explainability/**`

Unstaged:

- primary `pnpm-lock.yaml` remains dirty from prior work;
- `packages/resume-intelligence/package.json` remains unstaged;
- Portfolio Workspace release work remains unstaged;
- A16.2g documentation remains unstaged.

## 27. A16.2g Readiness Decision

NOT READY
