# A16.4c - Post-Authorization Release Hygiene and Clean Validation Reconciliation

## Document Control

- Status: Ready
- Date: 2026-08-12
- Authorization commit: `3dbb960cbad16b6b451199e35ffc03f41a272990`
- Commit message: `feat(portfolio-workspace): enforce production authorization ownership`
- Toolchain: Node `v22.23.2`, pnpm `11.9.0`

## Purpose

A16.4c reconciles the post-authorization validation state after the production authorization commit. The goal was to distinguish legitimate follow-up work from temporary validation artifacts, remove only proven disposable artifacts, and validate the committed authorization release candidate from a clean HEAD simulation.

No production behavior, Domain behavior, Application behavior, migration behavior, API public behavior, CI configuration, or idempotency behavior was changed in this slice.

## Authorization Commit Verification

The current authorization commit is:

- `3dbb960cbad16b6b451199e35ffc03f41a272990`
- `feat(portfolio-workspace): enforce production authorization ownership`

The commit remains the authoritative A16.4 authorization implementation. The index was empty during A16.4c review, and no staging or commit action was performed.

## Initial Dirty Worktree Assessment

The working tree contained legitimate follow-up work after the authorization commit, including Infrastructure migration-readiness work, idempotency follow-up files, release documentation, and untracked tests in other bounded contexts.

The working tree also contained temporary validation artifacts from prior clean-index and authorization-only simulations, plus generated build/cache directories. Those artifacts contaminated root validation because root lint, typecheck, and test discovery use broad repository globs.

## Temporary Validation Artifact Inventory

The following temporary validation directories were identified as disposable:

- `.a16-auth-index-sim-6121d7ff68ce4c24929e6a7856add7fa`
- `.a16-auth-only-sim-101423a8007b4b94808d23fb62333444`
- `.a16-auth-validation-clean-sim`
- `a16-auth-validation-clean-sim`

These were copied repository simulations under the repository root. They were not legitimate source, release documentation, migration artifacts, or follow-up implementation work.

## Generated Artifact Inventory

The following generated artifacts were identified as disposable for the purpose of restoring clean root validation semantics:

- `.next`
- `.turbo`
- `.pnpm-store`

`.pnpm-store` had been configured under the repository root and contained pnpm project metadata that allowed root test discovery to see copied simulation projects. Removing it did not change source behavior, lockfile state, package manifests, or dependency declarations.

## Legitimate Follow-Up Work Preserved

A16.4c preserved the legitimate dirty worktree, including:

- Infrastructure migration-readiness follow-up files;
- idempotency follow-up files;
- A16.2/A16.3/A16.4/A16.5 architecture records;
- untracked tests in other bounded contexts;
- package and Infrastructure files already present as local follow-up work.

No broad clean, reset, stash, or discard operation was performed.

## Root Validation Contamination Root Cause

Root validation was contaminated by temporary copied repositories and generated cache artifacts under the repository root:

- root lint scanned generated `.next` files inside copied simulation directories;
- root typecheck saw duplicate source trees and unrelated unstaged follow-up tests;
- root test discovery found tests inside temporary copied repositories and pnpm project metadata under `.pnpm-store`.

This was a validation-environment hygiene defect, not an authorization implementation defect.

## Cleanup Decision

Cleanup was limited to the proven disposable artifact inventory. No source files, migration files, repository files, documentation records, or legitimate follow-up files were removed.

Because copied `node_modules` hardlinks inside simulation directories blocked normal sandbox deletion, the explicit artifact removal was retried with elevated filesystem permission for only the enumerated disposable paths.

## Clean HEAD Simulation Method

The authorization commit was validated from an isolated clean HEAD simulation outside the repository working tree:

1. Exported `HEAD` with `git archive`.
2. Expanded the archive into a temporary directory outside `C:\Users\saura\saurabh-product-os`.
3. Ran validation from the exported clean source tree with Node `v22.23.2` and pnpm `11.9.0`.
4. Did not include unstaged follow-up files or temporary validation artifacts.

This method validated the committed authorization release candidate independently of the dirty working tree.

## Frozen Lockfile Result

`pnpm install --frozen-lockfile` passed in the clean HEAD simulation. The lockfile was accepted without package manifest or lockfile modification.

## Database-Free Validation Results

Clean HEAD package validation passed for:

- `@career-companion/portfolio-workspace`: build, lint, typecheck, test;
- `@career-companion/portfolio-workspace-application`: build, lint, typecheck, test;
- `@career-companion/infrastructure`: build, lint, typecheck, test, `db:check`;
- `@career-companion/api`: build, lint, typecheck, test.

Clean HEAD root validation passed for:

- `pnpm build`;
- `pnpm lint`;
- `pnpm typecheck`;
- `pnpm test`.

Live PostgreSQL suites were not rerun in the clean simulation because no test database URL was visible to the Codex process.

## Live PostgreSQL Evidence

The committed authorization source remains covered by previously accepted PostgreSQL 17 evidence:

- repository live integration: 17/17 passed;
- runtime live integration: 9/9 passed;
- API host live integration: 9/9 passed.

No production behavior changed after that evidence, so the live authorization result remains applicable.

## Authorization-Specific Live Evidence

The API host live suite covered the authorization-sensitive behavior:

- authorization ownership persisted in PostgreSQL;
- matching trusted principal can initialize and read;
- mismatched principal is denied;
- persisted `actorReference` remains audit provenance;
- `authorizationResourceReference` remains pseudonymous authorization state;
- privacy regression checks remain closed.

## Record Version 2 Deployment Condition

PortfolioExecution durable record version `2` is the first authorized durable shape. Version `2` stores `authorizationResourceReference` in the aggregate payload.

Legacy record version `1` remains intentionally unsupported by the mapper and fails closed because it cannot prove trusted authorization ownership. Before deploying against a database that may contain existing version `1` Portfolio Workspace rows, a production data assessment and explicit migration/backfill decision are required.

## Legacy Version 1 Assessment

A16.4c did not inspect production data and did not add an automatic migration. The current technical stance remains:

- new writes use record version `2`;
- version `1` reads fail closed;
- deployment with existing version `1` rows requires a separate operational migration decision.

## Public Forbidden / Not-Found Condition

The internal API may distinguish an existing-but-forbidden execution from a missing execution. That is acceptable for current internal handlers.

Before public exposure, Portfolio Workspace still needs a product/security decision on whether public responses should mask forbidden resources as not found to reduce enumeration risk.

## Authorization Defects Remaining

No authorization implementation defect remains open in A16.4c.

Remaining items are production-readiness conditions outside the authorization implementation:

- legacy record version `1` data assessment before deployment;
- public forbidden/not-found masking decision before public exposure;
- durable public mutation idempotency before public mutation routes;
- hosted CI and production operational governance as separate release gates.

## Documentation Reconciliation

A16.4b remains the commit verification record. This A16.4c document adds the clean validation reconciliation and closes the earlier dirty-root-validation condition.

## Readiness Decision

READY

A16.4 authorization is technically closed. The committed authorization source passes clean HEAD database-free validation, remains covered by accepted PostgreSQL 17 live evidence, and has no remaining authorization defects.
