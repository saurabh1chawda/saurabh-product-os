# A16.4d - Post-Authorization Follow-Up Work Isolation and Release Hygiene Review

## Document Control

- Status: Ready for release-hygiene approval
- Date: 2026-08-12
- Repository: `C:\Users\saura\saurabh-product-os`
- Authorization baseline: `3dbb960cbad16b6b451199e35ffc03f41a272990`
- Authorization baseline message: `feat(portfolio-workspace): enforce production authorization ownership`
- Scope: release hygiene, dirty-tree classification, and follow-up sequencing only

## Executive Summary

A16.4d reviewed the dirty worktree after the A16.4 authorization commit and A16.4c clean validation closure.

The authorization baseline is intact:

- HEAD is `3dbb960cbad16b6b451199e35ffc03f41a272990`;
- the index is empty;
- `git diff --check` passes with line-ending warnings only;
- A16.4c clean HEAD validation remains authoritative for the committed authorization implementation.

The remaining dirty worktree is not required for authorization correctness. It consists of coherent future idempotency work, migration/readiness changes that support that idempotency work, release-governance documentation, authorization closure documentation, unrelated bounded-context tests, and one stray generated text file.

Recommended A16.5 readiness model: **Model B - complete existing partial idempotency implementation**, with strict scope separation from authorization closure and unrelated bounded-context work.

## Authorization Baseline Verification

Verified commands:

```text
git rev-parse HEAD
git log -1 --oneline
git diff --cached --name-status
git diff --check
```

Results:

- `git rev-parse HEAD`: `3dbb960cbad16b6b451199e35ffc03f41a272990`
- `git log -1 --oneline`: `3dbb960c feat(portfolio-workspace): enforce production authorization ownership`
- staged index: empty
- `git diff --check`: passed with LF/CRLF warnings only

No staged files were present. A16.4d did not stage, commit, push, reset, clean, or stash.

## Dirty Worktree Inventory

### A. Authorization Documentation Follow-Up

- `docs/architecture/A16.4-production-authorization-validation-commit-preparation.md`
- `docs/architecture/A16.4b-production-authorization-commit-verification.md`
- `docs/architecture/A16.4c-post-authorization-release-hygiene-validation.md`
- `docs/architecture/A16.4d-post-authorization-follow-up-work-isolation-review.md`
- `docs/architecture/A16.2m-persisted-principal-privacy-contract-reconciliation.md`

These files record authorization preparation, commit verification, privacy reconciliation, and post-commit validation closure.

### B. Idempotency Implementation / Design

API/presentation contracts:

- `apps/api/src/index.ts`
- `apps/api/src/portfolio-workspace/idempotency/contracts.ts`
- `apps/api/src/portfolio-workspace/idempotency/index.ts`
- `apps/api/tests/portfolio-workspace-idempotency.test.ts`

Infrastructure persistence, mapper, store, schema, and tests:

- `packages/infrastructure/package.json`
- `packages/infrastructure/src/index.ts`
- `packages/infrastructure/src/portfolio-workspace/postgres/index.ts`
- `packages/infrastructure/src/portfolio-workspace/postgres/schema.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/PortfolioWorkspaceIdempotencyPersistenceContracts.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/PortfolioWorkspaceIdempotencyRecordMapper.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/PostgresPortfolioWorkspaceIdempotencyStore.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/index.ts`
- `packages/infrastructure/tests/portfolio-workspace-idempotency.test.ts`
- `packages/infrastructure/tests/portfolio-workspace-idempotency-postgres-live.test.ts`
- `packages/infrastructure/tests/portfolio-workspace-idempotency-integration-runner.ts`

Idempotency architecture records:

- `docs/architecture/A16.5-durable-mutation-idempotency-architecture.md`
- `docs/architecture/A16.5a-idempotency-contracts-replay-payload.md`
- `docs/architecture/A16.5b-durable-postgresql-idempotency-persistence.md`

### C. Migration / Persistence Follow-Up

Idempotency migration artifacts:

- `packages/infrastructure/drizzle/portfolio-workspace/0001_workable_molly_hayes.sql`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/0001_snapshot.json`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/_journal.json`

Migration readiness and harness updates:

- `packages/infrastructure/src/portfolio-workspace/runtime/PortfolioWorkspaceMigrationReadiness.ts`
- `packages/infrastructure/tests/portfolio-workspace-migration-readiness.test.ts`
- `packages/infrastructure/tests/portfolio-workspace-postgres-live.test.ts`
- `packages/infrastructure/tests/portfolio-workspace-postgres-schema.test.ts`
- `packages/infrastructure/tests/portfolio-workspace-postgres-test-harness.ts`
- `packages/infrastructure/tests/portfolio-workspace-runtime-live.test.ts`

These changes make the committed migration set include the idempotency table and update readiness/schema tests to expect two migrations and two tables.

### D. Record Version / Legacy Data Follow-Up

No dirty production source file is required for record version 2 authorization. HEAD already contains:

- `PORTFOLIO_EXECUTION_RECORD_VERSION = 2`;
- `aggregatePayload.authorizationResourceReference`;
- fail-closed unsupported-version behavior.

The legacy version 1 item remains an operational data-assessment condition, not an uncommitted code dependency.

### E. CI / Release Governance Follow-Up

- `docs/architecture/A16-portfolio-workspace-release-candidate-production-readiness-review.md`
- `docs/architecture/A16.2-ci-release-gates-postgresql-validation.md`
- `docs/architecture/A16.2k-portfolio-workspace-release-candidate-staging-review.md`
- `docs/architecture/A16.2k.1-release-candidate-drift-authorization-idempotency-reconciliation.md`
- `docs/architecture/A16.2k.2-authorization-idempotency-scope-separation.md`
- `docs/architecture/A16.2l-portfolio-workspace-release-candidate-commit.md`
- `docs/architecture/A16.2l-precommit-live-validation-reconciliation.md`
- `docs/architecture/A16.2m-hosted-ci-activation.md`
- `docs/architecture/A16.2m-release-candidate-state-reconciliation-restaging.md`
- `docs/architecture/A16.2n-github-actions-pnpm-ordering-correction.md`
- `docs/architecture/A16.2n-hosted-ci-validation-closure.md`
- `docs/architecture/A16.3-branch-protection-release-governance.md`

These document release-candidate staging, hosted CI, branch protection, and reconciliation history.

### F. Unrelated Bounded-Context Work

- `packages/career-intelligence/tests/deterministic-services.test.ts`
- `packages/career-knowledge/tests/aggregate-behavior.test.ts`
- `packages/decision-engine/tests/pipelines.test.ts`
- `packages/decision-model/tests/decision-model.test.ts`
- `packages/kernel/tests/kernel-primitives.test.ts`

These are unrelated tests for other packages and should not be mixed into Portfolio Workspace authorization or idempotency commits.

### G. Generated / Temporary / Stray

- `e-2-complete`
- `tatus --short --untracked-files=all`

The second file contains terminal/pager output and git warning text. It appears accidental and generated. A16.4d did not delete it because this slice is review-only.

### H. Uncertain

No source file remains uncertain after inspection. Some documentation files are broad release-governance records, but their conceptual category is clear.

## Existing Idempotency Work Inventory

Durable idempotency is already partially implemented across API and Infrastructure.

API/presentation owns:

- idempotency key value object;
- mutation operation vocabulary;
- authorization-resource scoped idempotency scope;
- deterministic fingerprint input and SHA-256 fingerprinting;
- safe replay payload;
- outcome and error vocabulary;
- database-free tests and public API exports.

Infrastructure owns:

- persistence contracts for reservation, completion, and release;
- idempotency record mapper;
- PostgreSQL idempotency store;
- Drizzle schema table `portfolio_workspace_idempotency_records`;
- SQL migration and Drizzle snapshot;
- migration readiness compatibility for the idempotency table;
- live PostgreSQL idempotency runner and tests.

Domain and Application remain free of idempotency imports in the inspected dirty work.

## Existing Migration / Persistence Work Inventory

The dirty migration and schema work is future idempotency storage:

- one new table: `portfolio_workspace_idempotency_records`;
- primary key: `scope_hash`;
- unique scope index on operation, authorization resource, resource identity, and idempotency key hash;
- hash validation constraints;
- replay success payload completeness constraint;
- expiration index;
- migration journal update from one committed migration to two migrations.

This does not alter `portfolio_executions` table shape and does not change PortfolioExecution record version 2.

## Record Version Follow-Up Assessment

Authorized record version 2 is already committed in HEAD and does not depend on dirty files.

Dirty idempotency files define a separate idempotency record version:

- `PORTFOLIO_WORKSPACE_IDEMPOTENCY_RECORD_VERSION = 1`

That idempotency record version is separate from:

- PortfolioExecution aggregate record version `2`;
- PostgreSQL migration versioning;
- optimistic-concurrency revision.

No current dirty file should be treated as an authorization record-version repair.

## Legacy Version 1 Assessment

Known behavior from HEAD:

- version 1 PortfolioExecution records are unsupported;
- mapper fails closed;
- no automatic migration or fallback ownership inference exists.

Repository evidence does not prove whether production version 1 rows exist. No production data access or assessment procedure was found in this slice.

Required follow-up: a separate production data-assessment/backfill decision before deployment to any database that could contain version 1 Portfolio Workspace rows.

## Public Forbidden / Not-Found Assessment

Current internal behavior:

- missing execution maps to not found;
- existing execution with mismatched ownership maps to forbidden;
- internal status mapping returns `404` for not-found and `403` for forbidden.

This distinction is currently internal-only. No public HTTP route implementation was inspected in this slice.

If exposed publicly, the distinction can create execution-ID enumeration risk. The policy should be resolved before public HTTP exposure, preferably in a public API security slice rather than by changing the internal authorization closure retroactively.

## Authorization Closure Integrity

A16.4c clean HEAD validation proves the committed authorization implementation works without any remaining dirty files.

No dirty source file is required for:

- persisted authorization ownership;
- trusted-principal derived initialization ownership;
- runtime authorization resource resolution;
- cross-principal denial;
- record version 2 mapper behavior;
- API host authorization behavior.

No hidden authorization dependency was found.

## Dirty File Dependency Assessment

Coherent idempotency dependency set:

- API idempotency contracts depend on existing API presentation contracts and `PortfolioWorkspaceAuthorizationResourceReference`;
- API root export depends on `apps/api/src/portfolio-workspace/idempotency/index.ts`;
- Infrastructure idempotency contracts are primitive and Infrastructure-owned;
- Infrastructure record mapper and store depend on the new schema row types;
- schema depends on idempotency replay-payload JSON type;
- migration readiness depends on the new idempotency table existing after migrations;
- test harness reset depends on deleting idempotency rows before execution rows;
- live idempotency command depends on the new package script.

Migration artifact dependency set:

- `0001_workable_molly_hayes.sql`;
- `meta/0001_snapshot.json`;
- `meta/_journal.json`;
- schema and readiness tests expecting two migrations.

Mixed documentation dependency set:

- release readiness docs now reference A16.4 authorization status, A16.5 idempotency progress, hosted CI, and branch protection. They should be reviewed as release-governance documentation, not as implementation source.

## Mixed Files

Mixed or broad files requiring careful commit grouping:

- `docs/architecture/A16-portfolio-workspace-release-candidate-production-readiness-review.md`: mixes authorization, CI/governance, and idempotency readiness updates.
- `packages/infrastructure/README.md`: documents idempotency persistence and updates the live harness description; belongs with idempotency persistence, not authorization closure.
- `packages/infrastructure/src/index.ts`: exports both existing Infrastructure runtime/repository API and new idempotency API additions; belongs with idempotency persistence.
- `packages/infrastructure/src/portfolio-workspace/postgres/schema.ts`: keeps existing PortfolioExecution table and adds idempotency table; should be committed with idempotency migration/schema as one coherent set.
- `packages/infrastructure/tests/*public API*`: many test files only update expected public API lists for idempotency exports; commit with idempotency exports.

No mixed file requires inclusion in an authorization closure commit.

## Unrelated Local Work

The unrelated bounded-context tests should be protected from Portfolio Workspace release commits:

- `packages/career-intelligence/tests/deterministic-services.test.ts`
- `packages/career-knowledge/tests/aggregate-behavior.test.ts`
- `packages/decision-engine/tests/pipelines.test.ts`
- `packages/decision-model/tests/decision-model.test.ts`
- `packages/kernel/tests/kernel-primitives.test.ts`

They may be valid future work, but they are outside A16.4/A16.5 Portfolio Workspace scope.

## Generated / Temporary Artifacts

No `.a16-auth-*`, `.next`, `.turbo`, or `.pnpm-store` artifacts were present after A16.4c cleanup.

The remaining stray file `tatus --short --untracked-files=all` appears generated from a mistyped shell command or redirected terminal output. It should be removed only in a future explicitly authorized cleanup slice.

## A16.5 Readiness Model

Selected model: **Model B - Complete Existing Partial Idempotency Implementation**.

Reasoning:

- API contracts are coherent and tested.
- Infrastructure persistence contracts, mapper, store, schema, migration, and live test harness are coherent.
- The implementation remains outside Domain/Application.
- The documented gaps are explicit: transaction coordination, handler integration, TTL/stale reservation policy, public idempotency-key enforcement, and live public retry validation.

Do not restart A16.5 from architecture-only. The existing work is more than fragments; it is a partial implementation that should be completed through controlled slices.

## Recommended Follow-Up Sequence

1. Commit authorization closure documentation only, if desired:
   - A16.4b;
   - A16.4c;
   - A16.4d;
   - optionally A16.4 preparation/privacy records if they are intended as durable history.
2. Preserve the current idempotency source as a future A16.5 work set; do not mix it with authorization docs.
3. Validate and finish A16.5a/A16.5b as a coherent idempotency contracts plus persistence slice, including the migration artifacts and live idempotency evidence.
4. Implement A16.5c transaction/handler integration as a separate slice.
5. Resolve public forbidden/not-found masking before public routes.
6. Resolve legacy record-version-1 production data assessment before deployment.
7. Keep unrelated bounded-context tests out of Portfolio Workspace release commits.

## Authorization Documentation Commit Recommendation

A separate documentation commit is recommended before continuing A16.5 implementation if the team wants a clean audit trail for authorization closure.

Recommended commit message:

```text
docs(architecture): close portfolio workspace authorization validation
```

This commit should contain only authorization closure/reconciliation documentation and must exclude idempotency implementation, idempotency docs, migration files, CI changes, and unrelated tests.

## Proposed Authorization-Docs Commit Files

Minimum recommended set:

- `docs/architecture/A16.4b-production-authorization-commit-verification.md`
- `docs/architecture/A16.4c-post-authorization-release-hygiene-validation.md`
- `docs/architecture/A16.4d-post-authorization-follow-up-work-isolation-review.md`

Optional if the project wants the full pre/post authorization trail:

- `docs/architecture/A16.4-production-authorization-validation-commit-preparation.md`
- `docs/architecture/A16.2m-persisted-principal-privacy-contract-reconciliation.md`

## Proposed Future Idempotency Scope Files

API:

- `apps/api/src/index.ts`
- `apps/api/src/portfolio-workspace/idempotency/contracts.ts`
- `apps/api/src/portfolio-workspace/idempotency/index.ts`
- `apps/api/tests/portfolio-workspace-idempotency.test.ts`

Infrastructure:

- `packages/infrastructure/README.md`
- `packages/infrastructure/package.json`
- `packages/infrastructure/src/index.ts`
- `packages/infrastructure/src/portfolio-workspace/postgres/index.ts`
- `packages/infrastructure/src/portfolio-workspace/postgres/schema.ts`
- `packages/infrastructure/src/portfolio-workspace/idempotency/**`
- `packages/infrastructure/tests/portfolio-workspace-idempotency*.ts`
- idempotency-related updates to Infrastructure public-API tests

Documentation:

- `docs/architecture/A16.5-durable-mutation-idempotency-architecture.md`
- `docs/architecture/A16.5a-idempotency-contracts-replay-payload.md`
- `docs/architecture/A16.5b-durable-postgresql-idempotency-persistence.md`

## Proposed Future Migration / Legacy Scope Files

Idempotency migration scope:

- `packages/infrastructure/drizzle/portfolio-workspace/0001_workable_molly_hayes.sql`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/0001_snapshot.json`
- `packages/infrastructure/drizzle/portfolio-workspace/meta/_journal.json`
- migration readiness/schema/harness tests that expect the idempotency table.

Legacy version 1 data scope:

- no implementation files currently identified;
- should start as a separate architecture/ops assessment before any migration/backfill code.

## Files Explicitly Excluded

Exclude from authorization docs and Portfolio Workspace idempotency commits unless separately approved:

- unrelated bounded-context tests under career-intelligence, career-knowledge, decision-engine, decision-model, and kernel;
- `e-2-complete`;
- `tatus --short --untracked-files=all`;
- unrelated release-governance docs not needed by the selected commit;
- any future generated cache/build directories.

## Remaining Production-Readiness Conditions

- Legacy record-version-1 production data assessment/backfill decision.
- Public forbidden/not-found enumeration policy before public route exposure.
- Durable public mutation idempotency transaction/handler integration.
- Provider authentication and production authorization operations.
- Observability, deployment, database operations, and runbooks.
- Hosted CI/governance evidence remains release-critical for future commits.

## Release Tree Risk

Risk: **MEDIUM**.

Reasons:

- The authorization baseline itself is clean and validated.
- The index is empty.
- The dirty tree contains coherent idempotency work but spans API, Infrastructure, schema, migration, tests, and docs.
- Some release docs are broad and mix status from multiple slices.
- Unrelated bounded-context tests and a stray generated file remain present.

The risk is manageable if the next commits preserve strict scope boundaries.

## Readiness Decision

READY FOR RELEASE-HYGIENE APPROVAL

A16.4d confirms the authorization closure is intact, classifies the remaining work, and recommends continuing with a scoped idempotency completion sequence rather than blending follow-up implementation into authorization closure.
