# A16.2c - Portfolio Workspace Isolated Lockfile Validation

## 1. Document Control

- Status: Not approved
- Date: 2026-08-09
- Repository: `C:\Users\saura\saurabh-product-os`
- Temporary candidate inspected: `C:\Users\saura\portfolio-workspace-release-candidate`
- Scope: failed isolated lockfile investigation and replacement decision
- Non-goals: staging, commit, push, primary worktree cleanup, production behavior changes, dependency upgrades, migration regeneration

## 2. Executive Summary

The first isolated lockfile attempt is not approved for replacement.

The temporary candidate lockfile was generated manually under the required toolchain:

- Node: `v22.23.2`
- pnpm: `11.9.0`

Manual commands passed:

- `pnpm install --lockfile-only`
- `pnpm install --frozen-lockfile`

However, the candidate was not isolated. It still included unrelated workspace importers:

- `packages/career-artifacts:`
- `packages/explainability:`
- `packages/resume-intelligence:`

Inspection confirmed the temporary candidate physically contains all copied workspace packages and retains the broad repository workspace membership:

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

As a result, pnpm correctly reported all workspace projects and regenerated a lockfile identical to the primary mixed lockfile.

A16.2d adds the final interpretation: this is not only a failed temporary-copy exclusion. `career-artifacts` and `explainability` are already present in `HEAD:pnpm-lock.yaml`, and committed package manifests already depend on them. The blocker is a workspace-scope / missing-package baseline issue that must be resolved before lockfile replacement.

## 3. First Isolation Failure Root Cause

The failure was not caused by pnpm.

The temporary candidate still physically contains:

- `packages/career-artifacts`
- `packages/explainability`
- `packages/resume-intelligence`

It also still uses the broad workspace glob:

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

Therefore pnpm included every `apps/*` and `packages/*` package visible in the candidate filesystem.

The resulting candidate lockfile has the same SHA-256 hash as the primary repository lockfile:

- Primary `pnpm-lock.yaml`: `CC571D39E48345995AFA143C5F407E0C47C1327E3E3887CE605162C720C5159A`
- Candidate `pnpm-lock.yaml`: `CC571D39E48345995AFA143C5F407E0C47C1327E3E3887CE605162C720C5159A`

This candidate must not replace the primary lockfile because it does not remove the mixed unrelated workspace entries.

## 4. Temporary Candidate Filesystem Assessment

The candidate contains many package directories, including the unrelated packages that A16.2a/A16.2b intended to exclude.

Observed package directories include:

- `packages/career-artifacts`
- `packages/explainability`
- `packages/resume-intelligence`
- `packages/portfolio-workspace`
- `packages/portfolio-workspace-application`
- `packages/infrastructure`
- `packages/infrastructure-memory`

The previous copy/exclusion approach did not produce an isolated filesystem. It recreated the same broad workspace graph.

## 5. Workspace Configuration Assessment

Candidate `pnpm-workspace.yaml`:

```yaml
packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true

overrides:
  expect-type: 1.3.0
```

This configuration is correct for the real repository, but in a temporary isolation tree it only works if unrelated package directories are absent.

Because unrelated directories were present, pnpm included them.

## 6. Workspace Dependency Graph Findings

The direct Portfolio Workspace dependency closure from:

- `@career-companion/api`
- `@career-companion/infrastructure`
- `@career-companion/infrastructure-memory`
- `@career-companion/portfolio-workspace`
- `@career-companion/portfolio-workspace-application`

contains 11 workspace packages:

- `apps/api` - Portfolio Workspace API host package
- `packages/infrastructure` - Portfolio Workspace durable/runtime infrastructure
- `packages/infrastructure-memory` - in-memory repository adapter
- `packages/portfolio-workspace` - Domain package
- `packages/portfolio-workspace-application` - Application package
- `packages/kernel` - required transitive workspace dependency
- `packages/persistence` - required by Infrastructure Memory
- `packages/repositories` - required by Infrastructure Memory
- `packages/retrieval` - required by Infrastructure Memory
- `packages/career-knowledge` - required by Infrastructure Memory / Repositories
- `packages/decision-model` - required by Infrastructure Memory

That direct closure does not require:

- `@career-companion/career-artifacts`
- `@career-companion/explainability`
- `@career-companion/resume-intelligence`

However, the real root workspace currently includes other tracked packages whose package manifests reference `career-artifacts`, `explainability`, and/or `resume-intelligence`.

Examples include:

- `packages/application-planner/package.json`
- `packages/ats-intelligence/package.json`
- `packages/career-decision/package.json`
- `packages/career-strategy/package.json`
- `packages/hiring-intelligence/package.json`
- `packages/interview-intelligence/package.json`
- `packages/interview-planner/package.json`
- `packages/job-intelligence/package.json`
- `packages/learning-planner/package.json`
- `packages/networking-planner/package.json`
- `packages/opportunity-intelligence/package.json`
- `packages/portfolio-intelligence/package.json`
- `packages/portfolio-planner/package.json`
- `packages/product-intelligence/package.json`
- `packages/resume-intelligence/package.json`

This matters because A16.2 CI currently runs root workspace validation with the real broad `pnpm-workspace.yaml`. A clean release checkout using the broad workspace must satisfy all included workspace package manifests, not only the Portfolio Workspace direct closure.

## 7. Required Workspace Packages

For a Portfolio Workspace-only dependency closure, retain:

- `apps/api`
- `packages/career-knowledge`
- `packages/decision-model`
- `packages/infrastructure`
- `packages/infrastructure-memory`
- `packages/kernel`
- `packages/persistence`
- `packages/portfolio-workspace`
- `packages/portfolio-workspace-application`
- `packages/repositories`
- `packages/retrieval`

For the current A16.2 GitHub workflow as written, the real clean checkout must also handle every workspace package discovered by:

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

That creates a release-candidate mismatch if unrelated packages are excluded while tracked package manifests still depend on them.

## 8. Excluded Workspace Packages

A16.2a/A16.2b intended to exclude:

- `packages/career-artifacts`
- `packages/explainability`
- unrelated `packages/resume-intelligence` edits
- unrelated bounded-context tests
- `packages/kernel/tests/kernel-primitives.test.ts`
- `e-2-complete`
- `"tatus --short --untracked-files=all"`

That exclusion remains valid for Portfolio Workspace source scope, but it is not yet compatible with the current broad root workspace validation contract unless the missing workspace-package dependency issue is resolved.

## 9. Corrected Isolation Method

No final corrected lockfile was generated in this slice.

A candidate-only narrowed `pnpm-workspace.yaml` could generate a Portfolio Workspace closure lockfile, but that lockfile would not honestly represent the current real repository checkout because the real `pnpm-workspace.yaml` remains broad and the A16.2 CI workflow runs root workspace commands.

The next correction must choose one architecture-consistent path:

1. include the workspace packages required by the broad root workspace dependency graph in the release candidate; or
2. change the release/CI validation scope so A16.2 does not claim broad root validation; or
3. complete and commit the missing workspace packages as a prerequisite shared release dependency if they are already required by tracked package manifests.

No such correction was made in A16.2c.

A16.2d selected the third finding as the active blocker: the missing package directories must either be approved as shared workspace baseline repair, or A16.2 must receive a separate workspace/CI scope correction.

## 10. Toolchain Used

Manual first attempt:

- Node: `v22.23.2`
- pnpm: `11.9.0`

Codex process:

- `node`: not visible
- `pnpm`: `11.16.0`

Codex did not run lockfile generation or frozen install.

## 11. Lockfile Regeneration Result

Manual first attempt:

- `pnpm install --lockfile-only`: passed
- generated candidate still included unrelated importers
- candidate lockfile is byte-identical to the primary mixed lockfile

Result:

- not approved

## 12. Frozen Lockfile Result

Manual first attempt:

- `pnpm install --frozen-lockfile`: passed

That proves the mixed candidate is internally consistent, but it does not prove release-candidate isolation.

## 13. Required Importer Verification

The failed candidate includes required importers:

- `apps/api:`
- `packages/infrastructure:`
- `packages/infrastructure-memory:`
- `packages/portfolio-workspace:`
- `packages/portfolio-workspace-application:`

It also includes unrelated importers, so importer verification is incomplete for approval.

## 14. Unrelated Importer Verification

The failed candidate still includes:

- `packages/career-artifacts:`
- `packages/explainability:`
- `packages/resume-intelligence:`

Therefore it fails isolation.

## 15. Required Dependency Verification

The failed candidate includes required dependency resolutions:

- `drizzle-orm@0.45.2`
- `drizzle-kit@0.31.10`
- `pg@8.22.0`
- `@types/pg@8.20.3`
- `expect-type@1.3.0`

These are necessary but not sufficient for approval because unrelated importers remain.

## 16. Candidate vs Primary Lockfile Assessment

The candidate and primary lockfiles have the same SHA-256 hash:

`CC571D39E48345995AFA143C5F407E0C47C1327E3E3887CE605162C720C5159A`

Therefore there is no candidate-vs-primary dependency improvement.

Required Portfolio Workspace entries remain present, but unrelated entries were not removed.

## 17. Unexpected Dependency Differences

No candidate-vs-primary dependency differences were observed because the files are identical.

The unexpected finding is structural:

- excluding `career-artifacts` and `explainability` from the release candidate conflicts with the broad root workspace if tracked non-Portfolio package manifests continue to require those workspace packages.

## 18. Candidate Lockfile Artifact

Current candidate artifact:

- `C:\Users\saura\portfolio-workspace-release-candidate\pnpm-lock.yaml`

Decision:

- not approved for replacement

## 19. Real Lockfile Replacement Decision

NOT APPROVED.

Do not replace:

- `C:\Users\saura\saurabh-product-os\pnpm-lock.yaml`

with:

- `C:\Users\saura\portfolio-workspace-release-candidate\pnpm-lock.yaml`

The candidate is identical to the current mixed lockfile.

## 20. Proposed Next Procedure

Before lockfile replacement approval, resolve the workspace-scope contradiction.

Recommended options for review:

1. Treat `career-artifacts` and `explainability` as required shared workspace packages for the release candidate because tracked package manifests already depend on them, then include them deliberately with their tests and lockfile entries.
2. If they must remain excluded, create a separate architecture/CI correction that narrows release validation to the Portfolio Workspace dependency closure and documents that A16.2 is not root-workspace validation.
3. If the tracked package manifests should not depend on missing workspace packages, correct those package manifests in their own non-Portfolio release-hygiene slice before A16.2 lockfile approval.

Do not hand-edit lockfile dependency blocks to hide this mismatch.

## 21. Documentation Changes

Created:

- `docs/architecture/A16.2c-portfolio-workspace-isolated-lockfile-validation.md`

A16.2a and A16.2b remain historically accurate and were not modified in this follow-up.

## 22. Guardrail Verification

This slice did not:

- stage files
- commit
- push
- reset
- clean the primary repository
- stash
- delete unrelated work
- replace the primary lockfile
- regenerate the lockfile in Codex
- use Node 24 or pnpm 11.16.0 to certify a candidate
- modify Domain/Application behavior
- modify Infrastructure/runtime behavior
- modify CI behavior
- regenerate migrations
- begin A16.3

## 23. Remaining Manual Actions

Manual replacement is blocked.

The next manual decision is not "copy the candidate lockfile"; it is choosing the correct release scope for workspace packages required by the broad root workspace.

## 24. Readiness Decision

NOT READY.

The failed candidate is internally installable but not isolated, and the real broad workspace currently creates a clean-checkout dependency issue if the unrelated workspace packages are excluded without further release-scope or CI-scope correction.
