# A16.2 - Portfolio Workspace CI Release Gates and PostgreSQL Validation

## 1. Document Control

- Status: RECONCILED - hosted release gates validated for A16.3 and PR #3
- Scope: CI release gates, PostgreSQL validation, and branch-protection evidence
- Repository: `C:\Users\saura\saurabh-product-os`
- A16.3 merge commit: `a350b00381424ee2bc7f1e2df35359073dcafd09`
- Foundation-test merge commit: `36855433acfc2060cf015e700c5e5cee5e55fec5`

## 2. Purpose

A16.2 converted manually proven Node 22 and PostgreSQL validation into a repeatable GitHub Actions release gate.

This record consolidates the later A16.2m, A16.2n, A16.2o, A16.2p, A16.3, and PR #3 evidence into the current CI source of truth. It does not redefine production capability beyond what the release line implemented.

## 3. Release Workflow

Workflow:

`.github/workflows/portfolio-workspace-release-validation.yml`

Triggers:

- pull requests
- pushes to `main`
- manual `workflow_dispatch`

Workflow permission posture:

- `contents: read`
- no production deployment action
- no production database credentials
- PostgreSQL credentials are CI-local and disposable

## 4. Toolchain Gate

The hosted workflow uses the repository Node and pnpm policy:

- Node from `.nvmrc`
- Corepack enabled
- pnpm `11.9.0`
- explicit pnpm version verification
- `pnpm install --frozen-lockfile`

The frozen-lockfile gate proves dependency resolution does not require lockfile mutation.

## 5. Static Release Validation

The `Release Validation` job runs:

- `pnpm build`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`

This validates the root application and workspace packages without requiring a live production database.

## 6. PostgreSQL 17 Integration Gate

The `Portfolio Workspace PostgreSQL Integration` job runs against a PostgreSQL 17 service container.

Coverage includes:

- Portfolio Workspace repository integration
- Portfolio Workspace idempotency integration
- Portfolio Workspace runtime integration
- Portfolio Workspace API-host integration
- Portfolio Workspace executable HTTP-host integration

The test database name is CI-local and explicitly validated as a test target.

## 7. A16.3 Post-Merge Evidence

PR #2 merged the release branch into `main` with merge commit `a350b00381424ee2bc7f1e2df35359073dcafd09`.

Authoritative post-merge workflow evidence:

- GitHub Actions run: `32454931699`
- `Release Validation`: passed
- `Portfolio Workspace PostgreSQL Integration`: passed
- executable HTTP-host PostgreSQL integration: passed
- Vercel production deployment: succeeded

This proves the merged Portfolio Workspace public-read/API-host release passed the required hosted gates.

## 8. PR #3 Foundation-Test Evidence

PR #3 merged Career Companion foundation tests into `main`.

Authoritative evidence:

- test commit: `f0897ed10ba66a07342408f9ea0317579490f2d8`
- merge commit: `36855433acfc2060cf015e700c5e5cee5e55fec5`
- post-merge workflow: `32465671419`
- `Release Validation`: passed
- `Portfolio Workspace PostgreSQL Integration`: passed
- Vercel status: succeeded
- test scope: 5 files, 678 insertions
- local candidate suites: 27 tests passed
- hosted root release validation: passed

This proves the foundation-test addition did not regress the hosted release gates.

## 9. Branch Protection and Required Checks

The branch-protection record established that the release gate should be treated as required release evidence.

The required check intent is:

- block routine merges when build, lint, typecheck, tests, PostgreSQL startup, repository integration, runtime integration, API-host integration, or executable HTTP-host integration fail;
- preserve a lightweight single-maintainer flow;
- avoid weakening checks through stale green runs, `continue-on-error`, or unreviewed bypasses.

## 10. Nonblocking GitHub Actions Annotation

GitHub emitted a Node-runtime annotation for some action runtime behavior during earlier hosted runs. The annotation did not cause the release workflow to fail and is classified as nonblocking unless GitHub later turns the annotation into a hard failure.

The repository should still monitor action-version/runtime compatibility during future CI maintenance.

## 11. What the Gates Prove

The gates prove:

- pinned toolchain activation works in hosted CI;
- frozen installation works;
- root build, lint, typecheck, and tests pass;
- PostgreSQL 17 integration works for the Portfolio Workspace repository, idempotency, runtime, API host, and executable HTTP host;
- merged A16.3 and PR #3 changes pass hosted release validation.

## 12. What the Gates Do Not Prove

The gates do not prove:

- public mutation endpoint correctness;
- deployed identity-provider configuration;
- production database backup, restore, or migration operations;
- production rate limiting;
- complete observability dashboards or alerts;
- complete Career Companion product readiness.

Those remain separate implementation and operations concerns.

## 13. Final Decision

CI and PostgreSQL validation are READY for the A16.3 public-read/API-host release scope and remain green after the PR #3 foundation-test merge.
