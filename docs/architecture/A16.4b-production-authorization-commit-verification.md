# A16.4b - Production Authorization Commit Verification

## Document Control

- Status: Reconciled by A16.4c
- Date: 2026-08-12
- Authorization commit: `3dbb960cbad16b6b451199e35ffc03f41a272990`
- Commit message: `feat(portfolio-workspace): enforce production authorization ownership`
- Toolchain observed by Codex: Node `v22.23.2`, pnpm `11.9.0`

## Commit Scope

The approved 64-file production authorization slice was committed as one atomic commit. The commit contains Domain authorization ownership state, Application initialization and authorization-resource resolver contracts, Infrastructure record version 2 mapping and runtime resolver exposure, API production authorization, and focused tests and architecture records.

The commit does not include idempotency follow-up files, migration/backfill follow-up files, package manifest changes, lockfile changes, CI changes, temporary validation artifacts, or the post-approval A16.4 validation notes.

## Pre-Commit Verification

Before commit, the staged index contained exactly 64 files with `2646 insertions(+)` and `84 deletions(-)`. `git diff --cached --check` passed, and no staged path matched idempotency, package manifest, lockfile, CI, or migration/backfill follow-up patterns.

Security-sensitive staged diff review confirmed:

- `authorizationResourceReference` is persisted as the ownership resource;
- `actorReference` remains audit provenance and is not used as authorization state;
- principal-derived ownership is pseudonymized with SHA-256;
- request-body ownership-shaped fields cannot override trusted ownership;
- unsupported version 1 records fail closed;
- forbidden responses do not expose stored ownership identifiers.

## Commit Verification

Post-commit verification confirmed:

- HEAD is `3dbb960cbad16b6b451199e35ffc03f41a272990`;
- commit message is exact;
- committed file count is exactly 64;
- commit stat is `2646 insertions(+)`, `84 deletions(-)`;
- `git diff HEAD^ HEAD --check` passed.

## Database-Free Validation

Post-commit package validation passed in the dirty worktree for:

- `@career-companion/portfolio-workspace`: build, lint, typecheck, test;
- `@career-companion/portfolio-workspace-application`: build, lint, typecheck, test;
- `@career-companion/infrastructure`: build, lint, typecheck, test, `db:check`;
- `@career-companion/api`: build, lint, typecheck, test.

`pnpm install --frozen-lockfile` passed.

Root build passed.

Root lint, root typecheck, and root test were run after commit but did not produce clean signals because the working tree contains untracked A16 clean-simulation directories and later unstaged idempotency/migration follow-up work. The failures were outside the authorization commit:

- root lint scanned generated `.next` files inside untracked simulation directories;
- root typecheck saw unstaged idempotency tests and duplicate simulation-source type identities;
- root test discovered tests inside untracked simulation copies and later unstaged migration/idempotency state.

A16.4a's clean staged-index database-free validation remains the authoritative clean validation for the committed authorization source.

## Live PostgreSQL Evidence

The committed source is byte-identical to the A16.4a staged authorization slice for which manual PostgreSQL 17 evidence was accepted:

- repository live integration: 17/17 passed;
- runtime live integration: 9/9 passed;
- API host live integration: 9/9 passed.

The Codex process did not rerun live PostgreSQL because `PORTFOLIO_WORKSPACE_TEST_DATABASE_URL` was not visible.

## Record Version and Rollout

Durable PortfolioExecution record version is `2`, and version 2 includes `authorizationResourceReference` in the aggregate payload. Aggregate revision remains separate optimistic-concurrency metadata.

Legacy version 1 records are intentionally unsupported and fail closed because they cannot prove trusted ownership. No automatic migration or ownership default was introduced. Deployment requires an explicit version 1 data assessment and migration/backfill decision if any production Portfolio Workspace rows exist.

## Public Exposure Condition

The internal API currently distinguishes missing executions from existing executions owned by another principal. This remains acceptable for the internal handler scope, but public exposure requires an explicit enumeration-risk policy decision before public routes are introduced.

## Readiness Decision

READY WITH CONDITIONS

The authorization commit is created and verified. Remaining conditions are outside the commit itself: clean or isolate the dirty validation artifacts/follow-up work before using root validation as a current-worktree signal, assess legacy production v1 data before deployment, and decide public not-found/forbidden masking before public exposure.

## A16.4c Reconciliation

A16.4c removed only proven disposable validation artifacts from the primary worktree and validated commit `3dbb960cbad16b6b451199e35ffc03f41a272990` from a clean HEAD simulation under Node `v22.23.2` and pnpm `11.9.0`.

Clean HEAD validation passed for frozen install, package build/lint/typecheck/test, Infrastructure `db:check`, and root build/lint/typecheck/test. The accepted PostgreSQL 17 live evidence remains applicable because no production behavior changed after the authorization commit.

The dirty-root-validation condition is closed by A16.4c. Remaining record-version deployment and public forbidden/not-found masking decisions are release conditions outside the A16.4 authorization implementation.
