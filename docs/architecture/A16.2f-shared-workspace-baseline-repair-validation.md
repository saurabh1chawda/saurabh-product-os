# A16.2f - Shared Workspace Baseline Repair Staging and Validation

## 1. Executive Summary

A16.2f staged only the approved shared workspace baseline repair source files for:

- `packages/career-artifacts`
- `packages/explainability`

The staged index is clean and contains only those two package trees. No Portfolio Workspace A10-A16 files, lockfile changes, CI files, documentation files, `resume-intelligence` files, or unrelated local work were staged.

Package-level validation for the two staged packages passed under Node `v22.23.2` and pnpm `11.9.0`.

However, clean `HEAD + staged baseline repair` frozen-lockfile validation failed because `HEAD:pnpm-lock.yaml` includes `@career-companion/career-artifacts` under the `packages/resume-intelligence` importer while `HEAD:packages/resume-intelligence/package.json` does not. The local working tree has a matching `resume-intelligence/package.json` dependency hunk, which explains why frozen install passes in the dirty primary worktree, but that hunk is not currently approved for the baseline repair.

A16.2f is therefore not ready for commit approval with only the two package trees staged.

Forward reference: A16.2g classifies the `resume-intelligence` mismatch as a HEAD lockfile baseline defect, not a missing `resume-intelligence` manifest dependency. The local `resume-intelligence` manifest hunk must remain unstaged; a narrow lockfile correction is the next required repair.

## 2. Approved Baseline Repair Scope

Approved staged scope:

- `packages/career-artifacts/**`
- `packages/explainability/**`

Excluded:

- generated/temp content;
- Portfolio Workspace A10-A16 work;
- `packages/resume-intelligence/**`;
- `pnpm-lock.yaml`;
- `pnpm-workspace.yaml`;
- CI workflow files;
- architecture docs.

## 3. career-artifacts File Inventory

Staged files:

- `packages/career-artifacts/README.md`
- `packages/career-artifacts/package.json`
- `packages/career-artifacts/tsconfig.json`
- `packages/career-artifacts/src/index.ts`
- `packages/career-artifacts/src/models/index.ts`
- `packages/career-artifacts/tests/career-artifacts.test.ts`

Package details:

- name: `@career-companion/career-artifacts`
- exports: `.`
- dependencies: `@career-companion/career-intelligence`, `@career-companion/career-knowledge`, `@career-companion/decision-model`, `@career-companion/explainability`, `@career-companion/kernel`
- scripts: `build`, `lint`, `typecheck`, `test`, `clean`

No Portfolio Workspace files exist under this package.

## 4. explainability File Inventory

Staged files:

- `packages/explainability/README.md`
- `packages/explainability/package.json`
- `packages/explainability/tsconfig.json`
- `packages/explainability/src/index.ts`
- `packages/explainability/src/alternative/index.ts`
- `packages/explainability/src/assembly/index.ts`
- `packages/explainability/src/confidence/index.ts`
- `packages/explainability/src/constraint/index.ts`
- `packages/explainability/src/evidence/index.ts`
- `packages/explainability/src/graph/index.ts`
- `packages/explainability/src/models/index.ts`
- `packages/explainability/src/narrative/index.ts`
- `packages/explainability/tests/explainability.test.ts`

Package details:

- name: `@career-companion/explainability`
- exports: `.`
- dependencies: `@career-companion/career-intelligence`, `@career-companion/career-knowledge`, `@career-companion/decision-engine`, `@career-companion/decision-model`, `@career-companion/kernel`
- scripts: `build`, `lint`, `typecheck`, `test`, `clean`

No Portfolio Workspace files exist under this package.

## 5. Dependency Compatibility Assessment

The staged package manifests use workspace dependencies already represented in `HEAD:pnpm-lock.yaml`.

The package sources do not import Portfolio Workspace A10-A16 code and do not require the local `resume-intelligence` source changes.

New finding:

`HEAD:pnpm-lock.yaml` is already inconsistent with `HEAD:packages/resume-intelligence/package.json`: the lockfile importer includes `@career-companion/career-artifacts`, but the committed manifest does not. This is outside the approved two-package staging scope.

## 6. Files Staged

The Git index contains exactly 19 staged files:

- 6 files under `packages/career-artifacts`
- 13 files under `packages/explainability`

No other files are staged.

## 7. Files Explicitly Excluded

Excluded from staging:

- `packages/resume-intelligence/**`
- `packages/portfolio-workspace/**`
- `packages/portfolio-workspace-application/**`
- Portfolio Workspace Infrastructure/API changes
- `docs/architecture/A10-A16*`
- `.github/workflows/**`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`
- `packages/kernel/tests/kernel-primitives.test.ts`
- unrelated package tests
- `e-2-complete`
- `"tatus --short --untracked-files=all"`

## 8. Index Integrity Result

Passed.

Commands:

- `git diff --cached --name-status`
- `git diff --cached --stat`
- `git diff --cached --check`
- `git status --short`

Result:

- staged files are limited to approved baseline package files;
- staged diff check passed;
- line-ending warnings are informational only.

## 9. Node Runtime

Node runtime visible to Codex:

- `v22.23.2`

## 10. pnpm Runtime

pnpm runtime visible to Codex:

- `11.9.0`

## 11. Frozen Lockfile Result

Primary dirty worktree:

- `pnpm install --frozen-lockfile` passed.
- It used pnpm `11.9.0`.
- It did not require staging `pnpm-lock.yaml`.

Clean `HEAD + staged baseline repair` simulation:

- failed with `ERR_PNPM_OUTDATED_LOCKFILE`.
- failure reason: `HEAD:pnpm-lock.yaml` includes `@career-companion/career-artifacts` under `packages/resume-intelligence`, but `HEAD:packages/resume-intelligence/package.json` lacks that dependency.

This proves the two-package baseline repair is necessary but not sufficient for clean frozen-lockfile reproducibility.

## 12. career-artifacts Validation

Under Node `v22.23.2` and pnpm `11.9.0`:

- `pnpm --filter @career-companion/career-artifacts build`: passed.
- `pnpm --filter @career-companion/career-artifacts lint`: passed after rerun with normal filesystem access.
- `pnpm --filter @career-companion/career-artifacts typecheck`: passed.
- `pnpm --filter @career-companion/career-artifacts test`: passed after rerun with normal filesystem access; 1 file, 3 tests passed.

The first sandboxed test run failed to load the root Vitest config due to filesystem access restrictions; the normal-access rerun passed.

## 13. explainability Validation

Under Node `v22.23.2` and pnpm `11.9.0`:

- `pnpm --filter @career-companion/explainability build`: passed.
- `pnpm --filter @career-companion/explainability lint`: passed.
- `pnpm --filter @career-companion/explainability typecheck`: passed.
- `pnpm --filter @career-companion/explainability test`: passed; 1 file, 7 tests passed.

## 14. Root Build Result

Primary dirty worktree:

- `pnpm build` failed.
- Failure was in unstaged Portfolio Workspace Infrastructure code:
  `packages/infrastructure/src/portfolio-workspace/runtime/PortfolioWorkspaceMigrationReadiness.ts`.
- The error was unrelated to the staged baseline packages.

Clean `HEAD + staged baseline repair` simulation:

- root build was not run because frozen-lockfile validation failed first.

## 15. Root Lint Result

Not run after the clean frozen-lockfile blocker was discovered.

## 16. Root Typecheck Result

Not run after the clean frozen-lockfile blocker was discovered.

## 17. Root Test Result

Not run after the clean frozen-lockfile blocker was discovered.

`PORTFOLIO_WORKSPACE_TEST_DATABASE_URL` was not visible to the Codex process at validation time.

## 18. Lockfile Decision

Do not stage `pnpm-lock.yaml` for A16.2f.

The lockfile issue is not a missing two-package importer issue. The committed lockfile already contains both package importers and also contains a `resume-intelligence` importer dependency that does not match the committed `resume-intelligence/package.json`.

The next repair decision must choose one of:

1. include the `packages/resume-intelligence/package.json` dependency hunk as part of the shared baseline repair; or
2. correct `pnpm-lock.yaml` to remove that importer dependency; or
3. explain why the current committed mismatch is otherwise acceptable.

Do not decide that inside A16.2f without approval.

## 19. Clean-Checkout Confidence

Partial only.

Confidence is high that the staged two-package repair is clean and correct.

Confidence is not sufficient for commit approval because clean `HEAD + staged baseline repair` cannot pass frozen install until the `resume-intelligence` manifest/lockfile mismatch is resolved.

## 20. Files Created

- `docs/architecture/A16.2f-shared-workspace-baseline-repair-validation.md`

## 21. Files Modified

None for production/source behavior beyond staging the approved baseline files.

Documentation created but not staged:

- `docs/architecture/A16.2f-shared-workspace-baseline-repair-validation.md`

## 22. Documentation Changes

Created A16.2f validation record documenting:

- exact staged files;
- lockfile decision;
- toolchain;
- package validation;
- root validation blocker;
- index integrity;
- excluded work;
- clean-checkout confidence;
- commit recommendation status.

## 23. Remaining Risks

The staged repair alone is not enough for clean frozen-lockfile reproducibility.

The local `packages/resume-intelligence/package.json` hunk adding `@career-companion/career-artifacts` was previously classified as unrelated, but clean simulation now proves it corresponds to existing `HEAD:pnpm-lock.yaml` state.

That hunk needs a separate baseline-repair decision before commit approval.

## 24. Recommended Commit

Not recommended yet.

The intended eventual commit remains:

`fix(workspace): restore missing shared baseline packages`

But A16.2f should not proceed to commit approval until the `resume-intelligence` manifest/lockfile mismatch is resolved.

## 25. Guardrail Verification

Preserved:

- no commit;
- no push;
- no Portfolio Workspace files staged;
- no `pnpm-lock.yaml` staged;
- no `pnpm-workspace.yaml` staged;
- no `resume-intelligence` files staged;
- no source behavior modifications;
- no CI modifications;
- no cleanup/reset/stash;
- no A16.3 work.

Temporary validation artifacts were created outside the primary worktree and removed after use.

## 26. Git Diff Summary

Staged:

- `packages/career-artifacts/**`
- `packages/explainability/**`

Unstaged:

- existing Portfolio Workspace release work;
- existing unrelated local work;
- A16.2f documentation;
- lockfile/workspace manifest changes.

## 27. A16.2f Readiness Decision

NOT READY
